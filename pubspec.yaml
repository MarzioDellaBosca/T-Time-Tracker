name: flutter_tracker_application
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  table_calendar: ^3.0.3
  intl: ^0.17.0
  fl_chart: ^0.36.4
  http: ^0.13.3
  timer_builder: ^2.0.0
  encrypt: ^5.0.1
  window_manager: ^0.3.8
  shared_preferences: ^2.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
